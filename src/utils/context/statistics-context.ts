// DEPRECATED: This file is maintained for backward compatibility.
// Use the new store structure in @/store/slices/statistics instead.

export { useStatistics } from '@/store/slices/statistics';

// Note: StatisticsProvider is no longer needed with Zustand store
// If you need a provider, wrap your app with the store context
