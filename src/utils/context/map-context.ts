// DEPRECATED: This file is maintained for backward compatibility.
// Use the new store structure in @/store/slices/map instead.

export { useMap } from '@/store/slices/map';

// Note: MapProvider is no longer needed with Zustand store
// If you need a provider, wrap your app with the store context
